#!/usr/bin/env python
"""
Async collection classes stub for Phase 3.

These classes are placeholders for Phase 3 implementation.
They allow imports for type hints in Phase 2, but cannot be instantiated.
"""


class AsyncPrincipal:
    """Stub for Phase 3: Async Principal implementation."""

    def __init__(self, *args, **kwargs):  # type: ignore
        raise NotImplementedError(
            "AsyncPrincipal is not yet implemented. "
            "This is a Phase 3 feature (async collections). "
            "For now, use the sync API via caldav.Principal"
        )


class AsyncCalendarSet:
    """Stub for Phase 3: Async CalendarSet implementation."""

    def __init__(self, *args, **kwargs):  # type: ignore
        raise NotImplementedError(
            "AsyncCalendarSet is not yet implemented. "
            "This is a Phase 3 feature (async collections). "
            "For now, use the sync API via caldav.CalendarSet"
        )


class AsyncCalendar:
    """Stub for Phase 3: Async Calendar implementation."""

    def __init__(self, *args, **kwargs):  # type: ignore
        raise NotImplementedError(
            "AsyncCalendar is not yet implemented. "
            "This is a Phase 3 feature (async collections). "
            "For now, use the sync API via caldav.Calendar"
        )
